# 1 paso se debe ejecutar en la terminal el siguiente comando 
# docker build -t jsonserver .
# 2 paso se debe ejecutar en la terminal el siguiente comando 
# docker run --rm -it --name jsonserver-container -p 8080:8080 jsonserver